# Plugin Settings

## `previewSource`

- Type: string
- Default: `'@webroot'`

The path to the folder where preview images are stored. Aliases and environment variables are allowed.

## `useProjectConfig`

- Type: boolean
- Default: `true`

Whether to store content templates in Craft's project config. Disabling this allows content templates to be created in non-development environments.
